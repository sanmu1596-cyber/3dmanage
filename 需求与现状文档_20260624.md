# 裸眼3D游戏适配项目管理系统 — 需求与现状文档

> 文档版本：v1.1  
> 整理时间：2026-06-24  
> 整理人：AI助手  
> 审核人：乔老师

---

## 一、系统概述

### 1.1 业务背景

本系统服务于裸眼3D游戏适配团队，核心业务是将普通PC游戏通过 Reshade + Depth3D 方案改成 SBS（Side-By-Side）格式，利用 3D 交织软件进行左右交织，最终在裸眼3D屏幕上呈现立体游戏体验。

**团队架构：**
```
乔老师（团队Leader）
  ├── TPM（游戏适配）—— 对接开发人员，管理游戏适配进度
  ├── TPM（客户端）—— 对接开发人员，管理3D客户端版本
  ├── TPM（3D方案）—— 对接开发人员，管理交织方案版本
  └── 十几个测试人员 —— 执行测试任务，反馈问题
```

**核心痛点：**
- 目前用 Excel 管理，表格太乱太杂
- 很多信息可以互通，但分散在不同表格里
- 乔老师最关心的问题无法快速获取答案

### 1.2 乔老师最关心的四个问题

1. **某客户的游戏适配了多少？目前有哪些问题？版本准备好了吗？**
2. **某游戏还有什么问题？在不同客户那里表现一样吗？有什么解决方案？**
3. **客户经常遇到的问题以及快速解决方案，你们整理下？**
4. **老板想看游戏适配的进展以及计划情况等等**

### 1.3 技术架构

| 项目 | 说明 |
|------|------|
| 前端 | 原生 JS（模块化：core/auth/router/entities/plans/dashboard 等） |
| 后端 | Node.js + Express |
| 数据库 | SQLite3 |
| 认证 | 双模式（localhost 免登录 / 外网需登录） |
| 部署 | PM2 进程管理，线上地址 https://3dpm.testsite.woa.com |
| 本地地址 | http://localhost:3000 |

---

## 二、已完成功能模块

### 2.1 总览

| 模块 | 功能说明 | 状态 |
|------|---------|------|
| 项目概览（dashboard） | 项目整体数据看板，图表展示 | ✅ 已完成 |

### 2.2 基础数据模块

#### 游戏列表（games）
- **字段**：游戏名称、英文名、平台、游戏类型、开发商、运营商、发布日期、配置路径、适配进度、版本、包大小、适配状态（原hook开发状态）、在线状态、品质、游戏账号、存档位置、游戏引擎等
- **功能**：CRUD、行内编辑、字段设置（显隐/排序）、导入导出、筛选、排序
- **状态**：✅ 已完成，但「适配进度」是按游戏维度，未与客户打通

#### 设备列表（devices）→ 实为「客户列表」，需改名
- **字段**：厂商（=客户）、设备类型、设备名称、需求、数量、保管人、备注、适配完成率、总bug数、已完成适配数、总游戏数、状态、负责人
- **功能**：CRUD、行内编辑、拖拽排序、字段设置
- **重要**：「厂商」字段 = 客户名称；一个客户可能有多个设备
- **状态**：✅ 已完成，但需要统一改名为「客户列表」

#### 项目成员列表（members）
- **字段**：姓名、微信ID、项目角色、职责、状态
- **功能**：CRUD、行内编辑
- **状态**：✅ 已完成

### 2.3 游戏模块

#### 适配进展（progress）→ 关键模块
- **结构**：多个 sheet 页（每个 sheet = 一个客户），每个 sheet 下是游戏列表
- **字段**：游戏名称、适配状态（原「hook开发状态」）、品质、备注等
- **功能**：富文本备注（支持格式/颜色/对齐）、按客户筛选游戏适配状态、底部汇总统计（**未开始/开发中/已发布** 数量）
- **重要问题**：
  1. 累计适配数字是手动填的，未自动统计
  2. 游戏列表中的「适配进度」与这里的客户维度数据未打通
  3. 「Hook开发状态」需统一改名为「适配状态」，**状态值改为「未开始/开发中/已发布」**，底部汇总统计也同步修改
  4. 游戏名称可能重复，需**游戏名称 + 游戏平台**联合区分
- **数据底层**：存在 `tx_board_groups`（客户sheet）+ `tx_board_rows`（游戏行）表里，`cells` 字段是 JSON 格式
- **状态**：✅ 主体已完成，但数据打通和状态值修改未完成

#### 测试列表（tests）
- **状态**：✅ 已完成（具体功能需进一步确认）

#### 游戏问题（game-issues）
- **字段**：游戏名称、来源、问题类型、状态、优先级、描述、解决方案等
- **功能**：已关联游戏名称
- **状态**：✅ 已完成

#### 测试用例（test-cases）
- **字段**：用例编号、分类、优先级、前置条件、测试步骤、预期结果等
- **结构**：多个 sheet 页（全部用例/全局环境/未归档）
- **状态**：✅ 已完成

#### 配置计划（config-plan）
- **状态**：✅ 已完成（具体功能需进一步确认）

#### 版本管理（game-versions）
- **功能**：管理游戏的版本记录（版本号、状态、更新日志等）
- **状态**：✅ 已完成

### 2.4 交织模块

#### 交织问题（interlace-issues）
- **字段**：问题类型、版本、优先级、问题描述、负责人、状态、备注
- **状态**：✅ 已完成

#### 交织版本管理（interlace-versions）
- **状态**：✅ 已完成

### 2.5 客户端模块

#### 客户端问题（client-issues）
- **字段**：问题类型、版本、优先级、问题描述、负责人、状态、备注
- **状态**：✅ 已完成

#### 客户端版本管理（versions）
- **状态**：✅ 已完成

### 2.6 项目管理模块

#### 适配矩阵（matrix）
- **状态**：✅ 已完成（具体功能需进一步确认）

#### 缺陷列表（bugs）→ 需增强
- **字段**：设备名称、软件版本、缺陷状态、优先级、问题类型、描述等
- **重要问题**：
  1. **缺少「游戏名称」字段** → 无法与游戏列表打通
  2. **缺少「客户名称」字段** → 无法与客户列表打通
- **状态**：✅ 主体已完成，但需增加关联字段

#### 需求管理（requirements）→ TPM下发任务用
- **功能**：创建需求、下发任务
- **状态**：✅ 已完成

#### 管理者看板（admin-dashboard）
- **权限**：仅管理员可见
- **状态**：✅ 已完成

#### 操作日志（activity-logs）
- **功能**：记录所有操作（用户、动作、资源类型、变更内容、IP、User-Agent）
- **状态**：✅ 已完成，自动清理90天前日志

#### 我的任务（my-tasks）→ 测试人员工作台
- **功能**：查看被指派的任务是卡片式展示，带进度条和状态图标
- **测试人员工作入口**：测试人员主要使用两个页面：
  1. 「适配进展」页面 — 更新游戏的适配状态
  2. 「我的任务」页面 — 接收和查看任务
- **人员入口设计**：每个人的入口可以一样，在任务页面中筛选自己的名字即可（无需为不同角色设计不同首页）
- **状态**：✅ 已完成

#### 汇报报表（reports）→ 含多看板
- **子 tab**：各客户适配进展 / 腾讯系游戏开发进展 / 汇报报表
- **功能**：富文本看板（支持格式/颜色/对齐/合并单元格）
- **状态**：✅ 已完成（多看板架构已升级）

### 2.7 设置模块

#### 字段设置（field-settings）
- **功能**：控制各表的列显隐、排序、宽度
- **状态**：✅ 已完成

#### 用户管理（user-management）
- **功能**：用户CRUD、角色权限管理、权限矩阵
- **状态**：✅ 已完成

---

## 三、未完成/需增强的功能

### 3.1 高优先级（乔老师最关心的）

#### P0-1：数据打通 — 游戏↔客户↔Bug 双向关联

**现状：**
- 「适配进展」的数据存在 `tx_board_groups`（客户sheet）+ `tx_board_rows`（游戏行）表里
- `tx_board_groups` 存客户名称（title字段）、所属看板（board字段）
- `tx_board_rows` 存每行的游戏名称和状态（cells字段，JSON格式）
- **数据已经有**，但存在 JSON 里，需要和游戏列表、缺陷列表打通

**目标：**
- 在「游戏列表」中，增加「已适配客户」列，显示该游戏在哪些客户那边适配了（从 `tx_board_rows` 解析）
- 在「客户列表」中，「累计适配」数字自动从 `tx_board_rows` 统计（不再手动填）
- 在「缺陷列表」中，增加「游戏名称」和「客户名称」字段，实现双向跳转

**技术方案：**
1. **游戏列表增加「已适配客户」列：**
   - 后端新增接口：根据游戏名称+平台，查询 `tx_board_rows`（需解析 cells JSON 找到游戏名称匹配的行），返回对应的 `group_id`（客户）
   - 前端在游戏列表渲染时调用此接口，显示客户名称（可点击跳转到该客户的适配进展 sheet）

2. **客户列表「累计适配」自动统计：**
   - 后端新增接口：根据 `group_id`，查询 `tx_board_rows` 并解析 `cells` JSON，统计状态为「已发布」的行数
   - 前端在客户列表加载时调用此接口，自动填充「累计适配」字段（或改为实时计算，不存数据库）

3. **缺陷列表增加游戏名称、客户名称字段：**
   - 数据库 `bugs` 表增加 `game_id` 和 `device_id` 字段（外键）
   - 前端缺陷列表增加「游戏名称」下拉选择（从 games 表）和「客户名称」下拉选择（从 devices 表）
   - 支持点击游戏名称跳转到游戏详情，点击客户名称跳转到客户详情

**涉及模块：**
- 游戏列表（games）→ 增加「已适配客户」列
- 设备列表（devices）→ 累计适配数字自动统计
- 缺陷列表（bugs）→ 增加游戏名称、客户名称字段
- 适配进展（progress）→ 后端接口支持按游戏名称+平台查询

**注意事项：**
- `tx_board_rows.cells` 是 JSON 字符串，解析时需注意性能（游戏数量多时可考虑加索引或缓存）
- 游戏名称可能重复，需**游戏名称 + 游戏平台**联合区分（如「原神-Steam」、「原神-WeGame」）
- 建议后续考虑在 `tx_board_rows` 中增加 `game_id` 字段，直接关联 games 表，避免名称匹配问题

---

#### P0-2：适配状态统一修改

**目标：**
- 将「Hook开发状态」统一改名为「适配状态」
- 状态值从 `pending/in_progress/passed/failed` 改为 **「未开始/开发中/已发布」**
- 底部汇总统计同步修改（统计「未开始/开发中/已发布」的数量）

**涉及文件：**
- `tencent-games.js` — 前端渲染和状态值定义
- `tx_board_rows` 表 — 存量数据的状态值需要迁移（将旧状态值批量更新为新状态值）
- 底部汇总统计逻辑 — 需要改为按新状态值统计

**技术方案：**
1. 数据库迁移：将 `tx_board_rows` 中 `cells` JSON 里的旧状态值批量替换为新状态值
2. 前端修改：将状态值定义和显示文字改为「未开始/开发中/已发布」
3. 底部汇总统计：修改统计逻辑，按新状态值分类计数

---

#### P0-3：设备列表改名为客户列表

**目标：**
- 侧边栏、标题、所有相关文字统一改为「客户列表」
- 「厂商」字段改为「客户名称」

**涉及模块：**
- index.html 侧边栏
- devices 相关所有 JS、HTML、后端路由

**注意事项：**
- 数据库表名 `devices` 可以不改（影响太大），但前端显示文字全部改
- 后端路由 `/api/devices` 可以保持不动，只是前端显示改为「客户」

---

### 3.2 中优先级

#### P1-1：适配进展自动统计

**目标：**
- 每个客户的「累计适配」数字不再手动填，从游戏×客户适配记录中自动统计
- 底部汇总统计（未开始/开发中/已发布）自动计算
- **注**：此功能已在 P0-1 中包含，此处不再重复

#### P1-2：Bug影响范围区分

**目标：**
- Bug 能区分「仅影响某个客户」和「影响所有客户」
- 在游戏维度查看 Bug 时，能 filtering 出影响范围
- **实现方式**：
  - 在 `bugs` 表中增加 `scope` 字段（`single` = 仅某客户，`all` = 所有客户）
  - 或者利用已有的 `device_id` 字段（如果 `device_id` 为空，则表示影响所有客户）

#### P1-3：任务流程增强

**目标：**
- TPM 下发 → 测试执行 → 结果反馈 → 乔老师审批，形成完整闭环
- 当前「我的任务」是卡片式，任务状态流转需确认是否流畅
- **测试人员工作入口**：测试人员主要使用两个页面：
  1. 「适配进展」页面 — 更新游戏的适配状态
  2. 「我的任务」页面 — 接收和查看任务
- **人员入口设计**：每个人的入口可以一样，在任务页面中筛选自己的名字即可（无需为不同角色设计不同首页）

#### P1-4：汇报报表增强

**目标：**
- 老板想看的数据（游戏适配进展、计划情况）能有专门的报表视图
- 当前汇报报表是富文本看板，可能需要更结构化的数据展示

---

### 3.3 低优先级/隐形需求

#### P2-1：FAQ知识库

**目标：**
- 客户经常遇到的问题以及快速解决方案，整理成知识库
- 测试人员和TPM可以快速查询

#### P2-2：测试报告自动生成

**目标：**
- 测试完成后，能自动生成测试报告（基于测试用例执行结果）

#### P2-3：通知提醒功能

**目标：**
- 任务下发后有通知
- Bug状态变更后有通知
- 当前有通知按钮（🔔），但功能需确认

---

## 四、隐形需求（乔老师可能没明确说，但我推测需要的）

### 4.1 数据一致性保障

- 现在多个地方的数据是手动维护的（如适配进展的累计适配数字），容易出现不一致
- 需要建立数据源头，其他地方自动计算或引用

### 4.2 权限细化

- 测试团队上线后，不同角色（TPM/测试人员）看到的数据和操作权限需要更细化
- 当前有权限矩阵，但需确认是否满足测试团队的需求

### 4.3 移动端支持

- 测试人员可能在实验室测试，需要能用手机快速查看任务和反馈问题
- 当前系统是否是响应式？需确认

### 4.4 数据导入/迁移

- 现有 Excel 数据需要能批量导入系统
- 当前有导入导出功能，但需确认是否支持测试团队现有的 Excel 格式

### 4.5 性能优化

- 游戏列表已有109个游戏，后续会更多
- 测试团队上线后，并发用户数会增加
- 需关注系统性能（当前有 gzip 压缩、骨架屏等优化）

---

## 五、后续开发优先级建议

### 第一阶段（测试团队上线前必须完成）
1. **P0-2：适配状态统一修改** — 改状态值和显示文字，成本低
2. **P0-3：设备列表改名为客户列表** — 改文案，成本低
3. **P0-1：数据打通** — 游戏↔客户↔Bug 双向关联，工作量较大但最关键

### 第二阶段（测试团队上线后1个月内）
4. **P1-2：Bug影响范围区分** — 支持「仅某客户」和「所有客户」两种Bug
5. **P1-3：任务流程增强** — 确保TPM→测试人员的闭环流畅

### 第三阶段（后续迭代）
6. **P1-4：汇报报表增强** — 给老板看的数据更直观
7. **P2-1：FAQ知识库** — 客户常见问题快速查询
8. **P2-2：测试报告自动生成** — 提升效率
9. **P2-3：通知提醒功能** — 任务/Bug状态变更自动通知

---

## 六、关键技术决策记录

### 6.1 多看板架构

- 汇报报表已升级为多看板架构（board 区分符）
- 可复用此架构创建新看板
- 相关文件：`tencent-board.js`（后端）、`tencent-games.js`（前端）、`router.js`

### 6.2 富文本编辑器

- 适配进展和汇报报表使用自研富文本编辑器
- 近期修复了多个bug（列表重复增生、对齐失效、拖选退出等）
- 已移除列表功能（v=20260617e）
- 批量格式用 DOM wrapper（tx-cellfmt）而非正则

### 6.3 字段设置系统

- 所有表的列显隐、排序、宽度都支持用户自定义
- 配置文件存在 `field_options` 表中

### 6.4 数据底层结构（适配进展）

- `tx_board_groups` 表：存客户sheet（title=客户名称，board=所属看板）
- `tx_board_rows` 表：存游戏行（cells=JSON格式，包含游戏名称、适配状态等）
- **重要**：`cells` 是 JSON 字符串，解析需注意性能；建议后续增加 `game_id` 字段直接关联 games 表

---

## 七、附录：系统菜单完整结构

```
总览
  └── 项目概览

基础数据模块
  ├── 游戏列表
  ├── 设备列表（需改名为「客户列表」）
  └── 项目成员列表

游戏模块
  ├── 适配进展
  ├── 测试列表
  ├── 游戏问题
  ├── 测试用例
  ├── 配置计划
  └── 版本管理

交织模块
  ├── 交织问题
  └── 版本管理

客户端模块
  ├── 客户端问题
  └── 版本管理

项目管理
  ├── 适配矩阵
  ├── 缺陷列表（需增加游戏名称、客户名称字段）
  ├── 需求管理
  ├── 管理者看板
  ├── 操作日志
  ├── 我的任务
  └── 汇报报表

设置
  ├── 字段设置
  └── 用户管理
```

---

*本文档基于系统代码和乔老师的描述整理，如有遗漏或误解，请以乔老师审核为准。*
